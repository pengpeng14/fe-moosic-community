import styled from "styled-components";
import { Typography } from "@mui/material";

const ErrorMessageStyle = styled(Typography)`
  color: #ee2c4d;
  font-size: 14px;
  text-transform: none;
`;

export default ErrorMessageStyle;
